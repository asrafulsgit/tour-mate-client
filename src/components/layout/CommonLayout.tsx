import type { ReactNode } from "react"

interface IProps {
    children : ReactNode
}

const CommonLayout = ({children } : IProps ) => {
  return (
    <div>
      
    </div>
  )
}

export default CommonLayout
